export const customStyles = {
    container: base => ({
        ...base,
        display: 'inline-block',
        width: '240px',
        margin: '0.5rem',
        scrollbarColor: 'gray lightgray',
        height: '3rem'
    }),
    valueContainer: (base) => ({
        ...base,
        height: '3rem',
    })
};
